---
title: "Beyond Single-Dimensional Compression: The Compound Sparsity Frontier of Large Language Models"
originalUrl: "https://arxiv.org/abs/2607.18280"
date: "2026-07-22T22:37:41.091Z"
---

# Beyond Single-Dimensional Compression: The Compound Sparsity Frontier of Large Language Models
# 超越单维压缩：大语言模型的复合稀疏性前沿

**Abstract:** Large language models (LLMs) are often compressed through static parameter pruning or dynamic token-level computation, yet aggressive sparsification can trigger rapid performance degradation beyond an essential sparsity boundary. This work asks \emph{whether combining these two mechanisms can delay such degradation by distributing the compression burden}.

**摘要：** 大语言模型（LLMs）通常通过静态参数剪枝或动态 Token 级计算进行压缩，然而，过度的稀疏化一旦超过必要的稀疏边界，往往会引发性能的迅速下降。本研究探讨了“通过结合这两种机制来分担压缩压力，是否能够延缓这种性能衰退”。

We study a minimalist compound sparsity framework that first applies low-rank approximation and channel pruning to obtain a statically compressed backbone, and then introduces lightweight routers for per-token dynamic layer skipping. This design enables independent control of parameter sparsity and token-level computation sparsity.

我们研究了一种极简的复合稀疏性框架：首先应用低秩近似和通道剪枝来获得一个静态压缩的主干网络，随后引入轻量级路由器实现逐 Token 的动态层跳过。这种设计使得参数稀疏性和 Token 级计算稀疏性能够被独立控制。

Experiments across language understanding and modeling benchmarks show that compound sparsity consistently outperforms single-mechanism compression under the same total sparsity, delaying the decay point on understanding tasks and preserving stronger modeling performance.

在语言理解和建模基准测试上的实验表明，在相同的总稀疏度下，复合稀疏性始终优于单一机制的压缩方法，它不仅延缓了理解任务中的性能衰减点，还保持了更强的建模性能。

Further analysis reveals cross-dimensional interference between parameter pruning and token skipping, and shows that near-balanced allocation is most effective under a fixed sparsity budget. These results demonstrate that compound compression provides a practical way to improve LLM compression, while revealing a broader cross-dimensional sparsity boundary that ultimately limits further compression.

进一步分析揭示了参数剪枝与 Token 跳过之间存在跨维度干扰，并表明在固定的稀疏度预算下，近乎平衡的分配策略最为有效。这些结果证明，复合压缩为提升 LLM 压缩效果提供了一种实用的途径，同时也揭示了一个更广泛的跨维度稀疏边界，该边界最终限制了进一步的压缩空间。

Code will be available at this https URL.
代码将在该链接提供。