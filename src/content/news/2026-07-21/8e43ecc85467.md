---
title: "A document QC tool — and how to check it"
originalUrl: "https://dev.to/preflight/a-document-qc-tool-and-how-to-check-it-1b1p"
date: "2026-07-20T22:24:30.017Z"
---

### A document QC tool — and how to check it
### 文档质量控制工具及其检测方法

A check that cannot be determined from the file says "not evaluable" — never a silent pass. The problem: It looks fine right up until it doesn't — in a viewer, on screen, in the happy path — and the failure only shows at the point where it's expensive to fix.
如果一项检查无法根据文件本身得出结论，它会显示为“无法评估”（not evaluable），绝不会默认通过。问题在于：在查看器、屏幕显示或理想路径下，文件看起来一切正常，直到出现问题时，你才发现故障，而此时修复的成本已经非常高昂。

What actually happens: This is a determinable property of the file: you can check it deterministically, without running the document and without guessing. Preflight does exactly that as part of its correctness-first pipeline.
实际情况是：这其实是文件的一个可确定属性。你可以在不运行文档、不进行猜测的情况下，以确定性的方式对其进行检查。Preflight 正是将其作为“正确性优先”流程的一部分来执行此操作。

The honest caveat: `not_evaluable` is a first-class verdict; it never counts as a pass. That distinction is the whole point of the tool — a check that can't be determined from the file says "not evaluable," never a silent pass. A QC tool that passes everything hasn't checked your document; it has flattered it.
诚实的警示：“无法评估”是一个一等判定结果；它绝不计入“通过”。这种区分正是该工具的核心意义所在——无法根据文件确定的检查项会显示为“无法评估”，绝不会静默通过。一个对所有文件都显示“通过”的质量控制工具，并没有真正检查你的文档，它只是在敷衍了事。

Try it: Preflight parses the print formats most tools can't open (PDF, AFP/MO:DCA, PCL 5, PostScript, IPDS) and runs deterministic checks like this one — free, in your browser, nothing uploads. → https://preflightdocs.com
立即尝试：Preflight 可以解析大多数工具无法打开的打印格式（PDF、AFP/MO:DCA、PCL 5、PostScript、IPDS），并运行此类确定性检查。完全免费，直接在浏览器中运行，无需上传任何文件。→ https://preflightdocs.com