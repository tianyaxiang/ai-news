---
title: "GRADE: Single-Frame Generative Radar Depth Estimation Under Visual Degradation"
originalUrl: "https://arxiv.org/abs/2609.10756"
date: "2026-09-11T23:35:14.206Z"
---

# GRADE: Single-Frame Generative Radar Depth Estimation Under Visual Degradation
# GRADE：视觉退化环境下的单帧生成式雷达深度估计

**Abstract:** Dense 3D depth perception fails under smoke, fog, and darkness because optical sensors cannot penetrate airborne particulates. mmWave radar remains usable and measures range accurately under these conditions, but its small aperture limits angular resolution. 

**摘要：** 在烟雾、浓雾和黑暗环境下，密集的 3D 深度感知往往会失效，因为光学传感器无法穿透空气中的微粒。毫米波雷达在这些条件下依然可用并能精确测量距离，但其较小的孔径限制了角分辨率。

We present GRADE, which grounds a pretrained generative prior in single-frame radar geometry to estimate high-fidelity metric depth. GRADE first maps raw 4D radar spectra to coarse metric depth. A latent diffusion backbone then recovers structural detail while conditioning every denoising step on this estimate. 

我们提出了 GRADE，它通过将预训练的生成先验与单帧雷达几何结构相结合，从而估计出高保真的度量深度。GRADE 首先将原始的 4D 雷达频谱映射为粗略的度量深度。随后，一个潜在扩散（latent diffusion）主干网络在恢复结构细节的同时，将该估计值作为每个去噪步骤的条件。

A pixel-space adapter uses residual camera cues when available and is trained across clear, smoke-degraded, and occluded inputs so the full output approaches the radar-conditioned path as visibility degrades. 

像素空间适配器在可用时会利用残余的相机线索，并针对清晰、烟雾退化和遮挡的输入进行训练，使得当能见度降低时，最终输出会趋向于以雷达为条件的路径。

Trained and evaluated on ~95K frames across 12 buildings with real smoke, GRADE achieves an MAE of 0.303 m in clear scenes and 0.313 m under smoke, outperforming existing baselines. Code and datasets are available at this https URL.

GRADE 在 12 栋建筑中约 9.5 万帧真实烟雾场景下进行了训练和评估，在清晰场景中实现了 0.303 米的平均绝对误差（MAE），在烟雾环境下为 0.313 米，性能优于现有的基准模型。代码和数据集可在该链接获取。