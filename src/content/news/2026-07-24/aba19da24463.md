---
title: "Why Adding More AI Agents Made Our System Slower"
originalUrl: "https://towardsdatascience.com/why-adding-more-ai-agents-made-our-system-slower/"
date: "2026-07-23T22:36:35.876Z"
---

# Why Adding More AI Agents Made Our System Slower
# 为什么增加更多的 AI Agent 反而让我们的系统变慢了

Agentic AI: The hidden cost of asynchronous systems, how tiny CPU tasks quietly became our biggest bottleneck while scaling hundreds of LLM agents.
智能体 AI：异步系统的隐形成本——在扩展数百个 LLM Agent 时，微小的 CPU 任务是如何悄然成为我们最大瓶颈的。

You’re building a product around LLMs. You’re working on a previously impossible challenge that is now possible to tackle using LLMs. You deploy the first few agents into your system, and everything seems fine. Customers are happy. The responses aren’t the fastest, LLMs tend to be quite slow, but it’s manageable.
你正在围绕大语言模型（LLM）构建产品。你正在攻克一个曾经不可能、但现在借助 LLM 可以解决的挑战。你将最初的几个 Agent 部署到系统中，一切看起来都很顺利。客户很满意。虽然响应速度不算最快（毕竟 LLM 本身就比较慢），但还在可控范围内。

As you start deploying more and more agents, however, you begin noticing strange anomalies. Slowly but steadily, latency starts increasing. At first, you assume it’s the LLM providers. Maybe the new OpenAI model is slower. Maybe Anthropic is under heavy load. LLMs are inherently slow anyway, right?
然而，随着你部署的 Agent 越来越多，你开始注意到一些奇怪的异常现象。延迟开始缓慢但持续地增加。起初，你以为是 LLM 提供商的问题。也许是 OpenAI 的新模型变慢了？也许是 Anthropic 的负载过重？反正 LLM 本身就很慢，对吧？

Then you start seeing timeout errors. When you dive into the logs and metrics, something doesn’t add up. OpenAI claims the requests are completing very quickly, yet your agent is taking an extremely long time to respond. “OpenAI must be lying to us!” Then comes the painful realization. OpenAI wasn’t the problem. Our code was.
接着，你开始看到超时错误。当你深入查看日志和指标时，发现有些不对劲。OpenAI 声称请求完成得非常快，但你的 Agent 响应却耗时极长。“OpenAI 一定是在骗我们！”随后，痛苦的真相浮出水面：问题不在 OpenAI，而在我们自己的代码里。

At Planck, we went through exactly that transition. As our agent ecosystem grew, so did our response times. This is the story of how we discovered the real bottleneck and how we solved it. If you’re starting to use LLM agents in production and wondering how they will scale, if you’re an AI engineer, or if you simply enjoy challenging asynchronous system design problems, I invite you to follow along.
在 Planck，我们经历了完全相同的过程。随着我们的 Agent 生态系统不断壮大，响应时间也随之增加。这就是我们如何发现真正瓶颈并解决它的故事。如果你正开始在生产环境中使用 LLM Agent 并思考如何扩展，或者你是一名 AI 工程师，又或者你单纯喜欢挑战异步系统设计问题，欢迎跟随本文一探究竟。

### The Setup
### 系统架构

At Planck, we had several LLM agents running in production. They were all served by a single service, meaning that a single request, whether via HTTP or a worker queue, would trigger all the agents simultaneously. Each agent would then make dozens of calls to its own sub-agents.
在 Planck，我们的生产环境中运行着多个 LLM Agent。它们由同一个服务提供支持，这意味着单个请求（无论是通过 HTTP 还是工作队列）都会同时触发所有 Agent。随后，每个 Agent 又会向其子 Agent 发起数十次调用。

At first glance, this architecture looks ideal. Almost all the work is I/O bound, so using Python’s async ecosystem allows every agent to execute concurrently. The latency of a request should therefore be dominated by the slowest LLM call, not by the total number of agents. At least, that’s what we expected.
乍一看，这种架构非常理想。几乎所有的工作都是 I/O 密集型的，因此使用 Python 的异步生态系统可以让每个 Agent 并发执行。因此，请求的延迟应该由最慢的那个 LLM 调用决定，而不是由 Agent 的总数决定。至少，我们是这么预期的。

### Zooming In
### 深入分析

Why could that be? We were using async exactly as we should. All of the agents and sub-agents were supposed to run simultaneously, so in theory there should have been little to no difference between running one agent and running 10,000 agents. When we run the code in debug mode, we see event loop lag warnings, which means the task is finished, i.e the LLM returns a result, but the event loop can’t acquire a worker to look at it because it is busy doing something else.
为什么会这样？我们完全按照规范使用了异步编程。所有的 Agent 和子 Agent 本应同时运行，理论上运行一个 Agent 和运行一万个 Agent 应该没什么区别。当我们以调试模式运行代码时，看到了“事件循环延迟”（event loop lag）警告。这意味着任务已经完成（即 LLM 返回了结果），但事件循环因为忙于处理其他事情，无法获取工作线程来处理该结果。

But why can’t the event loop acquire a worker to handle the LLM response? In Python, because of the GIL, only one thread can execute code at a time. That means the event loop must be busy executing some CPU-bound work elsewhere. But what could it be? To figure that out, we should use a profiler.
但为什么事件循环无法获取工作线程来处理 LLM 的响应呢？在 Python 中，由于全局解释器锁（GIL）的存在，同一时间只能有一个线程执行代码。这意味着事件循环一定是在忙于执行其他地方的 CPU 密集型任务。那会是什么呢？为了找出答案，我们需要使用性能分析器（Profiler）。

### The Easy Wins
### 轻松的优化

After reviewing the profiler, we can identify a few things. First, we waste a lot of time serializing and deserializing the body. By default, aiohttp uses the built-in json library, but we can significantly speed that up by using orjson. Second, we are not actually able to run all the I/O requests simultaneously. It turns out that the aiohttp session limits the number of HTTP connections we can open, and by default, that limit is 100.
查看性能分析报告后，我们发现了一些问题。首先，我们在序列化和反序列化请求体上浪费了大量时间。默认情况下，aiohttp 使用内置的 json 库，但我们可以通过使用 orjson 来显著提升速度。其次，我们实际上无法同时运行所有的 I/O 请求。事实证明，aiohttp 会话限制了我们可以打开的 HTTP 连接数，默认限制为 100。

So, we tried to increase and optimize this number, but not indefinitely. At some point, continuing to increase this value will hurt performance. And if we rerun the tests, we see the results are much better. However, even now, as the number of agents increases, the latency also increases.
因此，我们尝试增加并优化了这个数值，但不能无限增加。在某个临界点之后，继续增加该值反而会损害性能。重新运行测试后，结果确实好多了。然而，即便如此，随着 Agent 数量的增加，延迟依然在上升。

### The Real Bottleneck
### 真正的瓶颈

Some of you might blame...
你们中的一些人可能会责怪……