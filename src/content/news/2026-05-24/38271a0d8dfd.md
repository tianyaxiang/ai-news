---
title: "Finderlock"
originalUrl: "https://www.producthunt.com/products/finderlock"
date: "2026-05-23T22:58:55.867Z"
---

# Finderlock

**Finderlock: Lock Mac files with Touch ID - right in Finder**
**Finderlock：使用 Touch ID 直接在 Finder 中锁定 Mac 文件**

Native macOS file protection with Touch ID integration. Lock and unlock files directly in Finder with one click - no separate app needed. AES-256 encryption, local-only storage.
原生 macOS 文件保护，集成 Touch ID 功能。无需额外应用程序，即可在 Finder 中一键锁定和解锁文件。采用 AES-256 加密，且仅在本地存储。

FinderLock is a native macOS app that lets you lock any file or folder in Finder with Touch ID or a password. AES-256 encryption — the same standard used by banks.
FinderLock 是一款原生 macOS 应用程序，允许您使用 Touch ID 或密码锁定 Finder 中的任何文件或文件夹。它采用 AES-256 加密技术，这是银行所使用的同等级别标准。

Unlike FileVault (full-disk only), FinderLock protects individual files with one click. Double-click to unlock, auto-lock when idle, and lock icon badges right in Finder.
与 FileVault（仅限全盘加密）不同，FinderLock 支持一键保护单个文件。双击即可解锁，支持闲置自动锁定，并在 Finder 中显示锁定图标徽标。

Everything stays on your Mac. No accounts, no cloud, no telemetry. Free plan included. One-time purchase — no subscription.
所有数据均保留在您的 Mac 上。无需账户、无需云端、无遥测数据。包含免费方案。一次性买断制，无需订阅。