---
title: "Hister - A private, full content search index that you control"
originalUrl: "https://hister.org/"
date: "2026-08-23T21:43:35.547Z"
---

# Hister - A private, full content search index that you control
# Hister - 一个由你掌控的私有全文搜索索引

The ultimate way to protect digital privacy hister.org/posts Keep useful pages close, searchable, and under your control... privacy-notes.md ~/documents/notes A private index makes the things you already found useful again.
保护数字隐私的终极方式 hister.org/posts 让有用的页面触手可及、可搜索，并完全由你掌控…… privacy-notes.md ~/documents/notes 一个私有索引能让你曾经发现的内容再次发挥价值。

Free software AGPLv3 Self hosted Run it on your own machine or server Privacy focused No telemetry, no external requests
自由软件 AGPLv3 协议，支持自托管，可在你自己的机器或服务器上运行，专注于隐私，无遥测，无外部请求。

Search the actual content Look beyond titles and URLs to the words inside every indexed document. Narrow with precision Use fields, phrases, wildcards, negation, priorities, and your own aliases. Read it in context Open a clean stored preview beside the results without losing your search.
搜索实际内容：超越标题和 URL，深入到每个已索引文档的内部文字。精确筛选：使用字段、短语、通配符、否定、优先级以及你自定义的别名。在上下文中阅读：在搜索结果旁打开简洁的存储预览，无需中断搜索过程。

Collect Save newly visited pages with the browser extension, watch local folders, import your history, or crawl a site. Visited pages Local files Index Hister extracts the parts that matter and indexes their full text on the server you choose. Full text Stored context
收集：通过浏览器扩展保存新访问的页面，监控本地文件夹，导入历史记录，或抓取网站。已访问页面、本地文件、索引：Hister 会提取关键部分，并在你选择的服务器上对其进行全文索引。全文、存储上下文。

Find Search from the web, terminal, command line, or let an AI assistant retrieve it through MCP. Web and terminal MCP assistants
查找：通过网页、终端、命令行进行搜索，或让 AI 助手通过 MCP（模型上下文协议）进行检索。网页与终端、MCP 助手。

No telemetry The server does not phone home or report what you search. No mandatory cloud A complete personal setup can run on one local machine. Your chosen server Clients send indexed content only to the Hister server you configure. Auditable software The source is public and licensed as free software under AGPLv3.
无遥测：服务器不会向外发送数据或报告你的搜索内容。无需强制云端：完整的个人设置可以在单台本地机器上运行。你选择的服务器：客户端仅将索引内容发送到你配置的 Hister 服务器。可审计的软件：源代码公开，并以 AGPLv3 自由软件协议授权。

Your sources Pages, files, and history you choose Your Hister server Extraction, rules, and searchable index Your answers Results, previews, and integrations
你的来源：你选择的页面、文件和历史记录。你的 Hister 服务器：提取、规则和可搜索索引。你的答案：结果、预览和集成。

Index automatically Browser extensions can index visited pages automatically. File watching, history imports, and the crawler add other sources. Browser extensions Local file watching History import Website crawler
自动索引：浏览器扩展可以自动索引已访问的页面。文件监控、历史记录导入和爬虫程序可添加其他来源。浏览器扩展、本地文件监控、历史记录导入、网站爬虫。

Search precisely Full text search supports field filters, quoted phrases, wildcards, negation, date ranges, and query aliases. Field filters Quoted phrases Wildcards and negation Query aliases
精确搜索：全文搜索支持字段过滤、引号短语、通配符、否定、日期范围和查询别名。字段过滤、引号短语、通配符与否定、查询别名。

Extract content Content extractors handle structured data from supported formats and websites. Semantic search is optional. Content extractors Semantic search Language aware indexes
提取内容：内容提取器处理来自支持格式和网站的结构化数据。语义搜索可选。内容提取器、语义搜索、语言感知索引。

Readable previews Apply index rules Skip and priority rules control indexing and ranking. Versioning can retain earlier document content. Skip rules Priority rules Version tracking Sensitive content checks
可读预览：应用索引规则。跳过和优先级规则控制索引与排名。版本控制可以保留文档的早期内容。跳过规则、优先级规则、版本追踪、敏感内容检查。

Use multiple interfaces The same index is available through the web interface, terminal client, CLI, HTTP API, and MCP server. Web interface Terminal interface HTTP API and CLI MCP server
使用多种界面：同一个索引可以通过网页界面、终端客户端、CLI、HTTP API 和 MCP 服务器访问。网页界面、终端界面、HTTP API 与 CLI、MCP 服务器。

Choose a deployment A single binary can run locally. Shared servers support user scoped access with SQLite or PostgreSQL. No config quickstart SQLite or PostgreSQL Multiple users Docker and Nix
选择部署方式：单个二进制文件即可在本地运行。共享服务器支持基于 SQLite 或 PostgreSQL 的用户范围访问。无配置快速启动、SQLite 或 PostgreSQL、多用户、Docker 和 Nix。