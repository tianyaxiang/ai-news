---
title: "Where Appearance Fails, Geometry Recognizes: A CAD-Free 3D Shape Prior That Complements Vision Foundation Models"
originalUrl: "https://arxiv.org/abs/2609.04381"
date: "2026-09-07T23:41:15.318Z"
---

# Where Appearance Fails, Geometry Recognizes: A CAD-Free 3D Shape Prior That Complements Vision Foundation Models
# 当外观失效时，几何结构来识别：一种补充视觉基础模型的无 CAD 3D 形状先验

**Abstract:** Recognizing specific objects onboarded without a labeled training set recurs across manufacturing and service robotics, yet the conventional renderable prior, a computer-aided-design (CAD) model, is often unavailable. Two-dimensional capture supplies no shape prior, and frozen foundation features fail on geometrically similar, low-texture industrial parts. We ask what a short object-centric scan buys for recognition beyond the captured images themselves: each object is reconstructed with 3D Gaussian Splatting (3DGS), summarized into a per-class shape prototype, and fused with frozen DINOv2 image features.

**摘要：** 在制造业和服务机器人领域，识别未经过标注训练集的新入库特定物体是一个反复出现的问题，然而传统的渲染先验——计算机辅助设计（CAD）模型——往往难以获取。二维图像采集无法提供形状先验，而冻结的基础模型特征在处理几何相似、纹理匮乏的工业零件时也会失效。我们探讨了简短的以物体为中心的扫描除了捕获图像本身外，还能为识别带来什么：每个物体通过 3D 高斯溅射（3DGS）进行重建，总结为类别的形状原型，并与冻结的 DINOv2 图像特征进行融合。

First, the scan recovers the recognition value of CAD without CAD: geometry from RGB-D depth (on T-LESS), 3DGS, and CAD gives comparable recognition (tied on HOPE, within 1.6 points on T-LESS); 3DGS is only a convenient route to a point cloud.

首先，这种扫描在没有 CAD 的情况下恢复了 CAD 的识别价值：来自 RGB-D 深度（在 T-LESS 数据集上）、3DGS 和 CAD 的几何信息提供了相当的识别效果（在 HOPE 数据集上持平，在 T-LESS 上差距在 1.6 个百分点以内）；3DGS 仅仅是获取点云的一种便捷途径。

Second, the payoff is governed by how recognizable the shape is: on shape-distinctive household objects (HOPE) geometry alone reaches 0.920 versus image-only 0.832, a ceiling below which fixed-weight fusion (0.872) sits. On shape-confusable textureless industrial parts (T-LESS) the gain is modest but consistent (0.560 to 0.591 fused, above both single signals).

其次，收益取决于形状的可识别程度：在形状独特的家居用品（HOPE）上，仅靠几何信息即可达到 0.920 的准确率，而仅靠图像则为 0.832，固定权重融合（0.872）则处于两者之间。在形状易混淆的无纹理工业零件（T-LESS）上，增益虽小但持续稳定（从 0.560 提升至融合后的 0.591，均高于单一信号）。

Third, the prior is complementary, not uniformly additive: it rescues far more image failures than it breaks successes, and its benefit grows under partial occlusion. Finally, the worth lies in geometry, not rendered pixels: 3DGS renderings do not help the image side, and frozen-feature recognition is nearly lighting-invariant (within 2.5 points). The study is scoped to recognition, not the BOP pose benchmark.

第三，这种先验是互补的，而非简单的线性叠加：它挽救的图像识别失败案例远多于其破坏的成功案例，且在部分遮挡情况下其优势更为显著。最后，其价值在于几何结构而非渲染像素：3DGS 渲染对图像侧没有帮助，且冻结特征的识别几乎不受光照影响（差异在 2.5 个百分点以内）。本研究仅限于识别任务，不涉及 BOP 位姿基准测试。