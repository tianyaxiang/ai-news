---
title: "PRISM: Priority-aware Rubric Internalization via Structured Multimodal Data Synthesis"
originalUrl: "https://arxiv.org/abs/2608.05249"
date: "2026-08-07T22:08:28.084Z"
---

### PRISM: Priority-aware Rubric Internalization via Structured Multimodal Data Synthesis
### PRISM：基于结构化多模态数据合成的优先级感知评分标准内化

**Abstract:** Real-world multimodal instructions often bundle multiple requirements with unequal importance, yet most multimodal training data still reduce instruction following to answering one self-contained question. We study this gap through **rubric comprehension**, which casts the model not as a generator measured against rubrics but as an **executor** that follows them: given an image and a typed, prioritized rubric, the model must verify each rule before producing an overall judgment.

**摘要：** 现实世界中的多模态指令往往包含多个重要性不等的具体要求，然而大多数多模态训练数据仍将指令遵循简化为回答单一的独立问题。我们通过“评分标准理解”（rubric comprehension）来研究这一差距，该方法不再将模型视为被评分标准评估的生成器，而是将其定位为遵循标准的“执行者”：给定一张图像和一个分类型的优先级评分标准，模型必须在做出最终判断之前验证每一条规则。

To support this setting, we propose **PRISM**, a four-stage data synthesis framework that produces persona--task pairs, prefix-guided rule sets, quality-filtered rubrics, and structured verification traces. We further introduce **PRISM-Eval**, whose Loose and Strict metrics use deterministic matching against fixed labels and therefore require no inference-time judge model.

为了支持这一设定，我们提出了 **PRISM**，这是一个四阶段数据合成框架，用于生成角色-任务对、前缀引导的规则集、经过质量过滤的评分标准以及结构化的验证轨迹。我们进一步引入了 **PRISM-Eval**，其“宽松”（Loose）和“严格”（Strict）指标通过与固定标签进行确定性匹配，因此无需在推理阶段使用额外的判别模型。

With only 10K synthesized samples, PRISM lifts Qwen3-VL-4B from 9.5% to 30.1% Strict accuracy on PRISM-Eval while preserving average performance on general benchmarks, and the gains transfer to four additional open-source MLLMs across dense and MoE architectures, suggesting that structured rubric supervision is a scalable path toward multi-rule, priority-aware multimodal instruction following.

仅使用 1 万个合成样本，PRISM 就将 Qwen3-VL-4B 在 PRISM-Eval 上的严格准确率从 9.5% 提升至 30.1%，同时保持了其在通用基准测试中的平均性能。这些增益还迁移到了其他四种涵盖稠密（dense）和混合专家（MoE）架构的开源多模态大模型（MLLM）上，这表明结构化的评分标准监督是实现多规则、优先级感知多模态指令遵循的可扩展路径。