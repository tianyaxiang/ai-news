---
title: "In-Context Forcing: Uncovering Context Effects in Autoregressive Video Diffusion"
originalUrl: "https://arxiv.org/abs/2608.05237"
date: "2026-08-07T22:08:40.858Z"
---

# In-Context Forcing: Uncovering Context Effects in Autoregressive Video Diffusion
# In-Context Forcing：揭示自回归视频扩散中的上下文效应

**Abstract:** Current few-step autoregressive video diffusion models depend on previous fully denoised clean frames as context for all denoising steps of the current frame. However, these clean frames leak excessive local details, which causes the model to take shortcuts, resulting in compromised temporal semantics and dynamics.
**摘要：** 当前的少步自回归视频扩散模型依赖于先前完全去噪的清晰帧，作为当前帧所有去噪步骤的上下文。然而，这些清晰帧泄露了过多的局部细节，导致模型走捷径，从而损害了时间语义和动态表现。

Inspired by the perspective of diffusion as masking, we explore the impact of noisy contexts on few-step autoregressive generation. Yet, simply applying contexts with the same noise levels provides insufficient guidance, leading to poor temporal consistency.
受“扩散即掩码（diffusion as masking）”这一视角的启发，我们探索了噪声上下文对少步自回归生成的影响。然而，简单地应用具有相同噪声水平的上下文无法提供足够的引导，导致时间一致性较差。

To resolve this dilemma, we introduce In-Context Forcing, a progressive autoregressive paradigm that utilizes contexts with decreasing noise levels. By applying less masking to distant frames and more masking to adjacent ones, this approach provides adaptive guidance, effectively ensuring both robust temporal consistency and high inter-frame dynamics.
为了解决这一困境，我们引入了“In-Context Forcing”，这是一种利用噪声水平递减的上下文的渐进式自回归范式。通过对远距离帧施加较少的掩码，并对相邻帧施加较多的掩码，该方法提供了自适应引导，有效地确保了稳健的时间一致性和高帧间动态性。

Furthermore, by decoupling the strict dependence on previous clean frames, our paradigm enables cross-frame parallel denoising, achieving substantial inference acceleration without sacrificing performance. Extensive experiments on VBench demonstrate that our method significantly outperforms state-of-the-art approaches in both visual fidelity and inference speed.
此外，通过解耦对先前清晰帧的严格依赖，我们的范式实现了跨帧并行去噪，在不牺牲性能的情况下实现了显著的推理加速。在 VBench 上的大量实验表明，我们的方法在视觉保真度和推理速度方面均显著优于当前最先进的方法。