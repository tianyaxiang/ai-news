---
title: "Don't Collapse Your Features: Why CenterLoss Hurts OOD Detection and Multi-Scale Mahalanobis Wins"
originalUrl: "https://arxiv.org/abs/2605.21493"
date: "2026-05-22T22:34:34.025Z"
---

# Don't Collapse Your Features: Why CenterLoss Hurts OOD Detection and Multi-Scale Mahalanobis Wins
# 不要压缩你的特征：为什么 CenterLoss 会损害 OOD 检测，而多尺度马氏距离更胜一筹

The ability to detect out-of-distribution (OOD) inputs is fundamental to safe deployment of machine learning systems. Yet, current methods often rely on feature representations that are optimised solely for classification accuracy, neglecting the distinct requirements of epistemic uncertainty.
检测分布外（OOD）输入的能力对于机器学习系统的安全部署至关重要。然而，目前的方法往往依赖于仅针对分类准确率进行优化的特征表示，而忽略了认知不确定性（epistemic uncertainty）的独特需求。

We introduce GOEN (Geometry-Optimised Epistemic Network), a simple pipeline that combines multi-scale features, L2 normalisation, Mahalanobis distance, and a calibration head trained with real hard OOD examples.
我们引入了 GOEN（几何优化认知网络），这是一个简单的流水线，结合了多尺度特征、L2 归一化、马氏距离以及使用真实困难 OOD 样本训练的校准头。

Through systematic ablation we uncover a counter-intuitive finding: CenterLoss, a popular regulariser for feature compactness, significantly degrades OOD detection performance, reducing average OOD AUROC from 0.9483 to 0.9366 despite improving classification accuracy.
通过系统的消融实验，我们发现了一个反直觉的结果：CenterLoss（一种用于特征紧凑性的流行正则化器）会显著降低 OOD 检测性能，尽管它提高了分类准确率，却将平均 OOD AUROC 从 0.9483 降低到了 0.9366。

The best variant, GOEN-NoCenterLoss, achieves an average OOD AUROC of 0.9483, surpassing all baselines including deep ensembles (0.8827), KNN (0.8967), and ODIN (0.8870) on CIFAR-10 benchmarks, while maintaining competitive in-distribution accuracy.
表现最好的变体 GOEN-NoCenterLoss 在 CIFAR-10 基准测试中达到了 0.9483 的平均 OOD AUROC，超过了所有基准方法，包括深度集成（0.8827）、KNN（0.8967）和 ODIN（0.8870），同时保持了具有竞争力的分布内准确率。

Our results challenge the prevailing assumption that better classification geometry automatically leads to better epistemic uncertainty. Instead, we show that overly tight feature clusters compress inter-class margins and distort the covariance structure needed for effective OOD detection.
我们的研究结果挑战了当前的一种普遍假设，即更好的分类几何结构会自动带来更好的认知不确定性估计。相反，我们证明了过于紧凑的特征簇会压缩类间边界，并扭曲有效 OOD 检测所需的协方差结构。

GOEN is efficient, training in under 20 minutes on a single GPU, and provides a practical blueprint for building AI systems that reliably recognise their own limitations.
GOEN 非常高效，在单块 GPU 上训练时间不到 20 分钟，并为构建能够可靠识别自身局限性的 AI 系统提供了一个实用的蓝图。