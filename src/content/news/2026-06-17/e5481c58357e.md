---
title: "Policy Regret for Embedding Model Routing: Contextual Bandits with Low-Rank Experts"
originalUrl: "https://arxiv.org/abs/2606.14929"
date: "2026-06-16T23:09:25.536Z"
---

# Policy Regret for Embedding Model Routing: Contextual Bandits with Low-Rank Experts
# 嵌入模型路由的策略遗憾：具有低秩专家的上下文多臂老虎机

**Abstract:** Modern recommendation systems increasingly rely on dynamically routing diverse queries to multiple embedding models. Despite its practical significance, this problem remains poorly understood under realistic conditions like adversarial queries, bandit feedback, and limited observability of models. 

**摘要：** 现代推荐系统越来越依赖于将多样化的查询动态路由至多个嵌入模型。尽管该问题具有重要的实际意义，但在对抗性查询、多臂老虎机反馈以及模型可观测性受限等现实条件下，人们对其理解仍然不足。

We formalize embedding model routing as an adversarial contextual linear bandit with low-rank experts, where contexts are queries, actions are items, and experts are the embedding models working on low-rank latent representation spaces. 

我们将嵌入模型路由形式化为一种具有低秩专家的对抗性上下文线性多臂老虎机问题，其中上下文为查询，动作为项目，而专家则是工作在低秩潜在表示空间中的嵌入模型。

We first establish that standard regret notions suffer from structural misspecification or statistical intractability, and we identify a log-quadratic policy class that is expressive enough to capture query-dependent model routing, yet structured enough to allow efficient online learning. 

我们首先确定了标准的遗憾概念会受到结构性错误设定或统计上难以处理的问题的影响，并确定了一类对数二次策略。该策略类既具有足够的表达能力来捕捉依赖于查询的模型路由，又具备足够的结构性以实现高效的在线学习。

Second, we propose a policy gradient algorithm called Hypentropy Policy Gradient (HPG). It provably adapts to the unknown low-rank structure under incomplete information and attains $\tilde{\mathcal O}(s\sqrt{M T})$ linearized policy regret -- where $s, M$, and $T$ are the intrinsic rank of the experts, the number of models, and the number of rounds -- thus avoiding a curse of dimensionality. 

其次，我们提出了一种称为“超熵策略梯度”（Hypentropy Policy Gradient, HPG）的算法。该算法被证明能够在信息不完整的情况下适应未知的低秩结构，并达到 $\tilde{\mathcal O}(s\sqrt{M T})$ 的线性化策略遗憾——其中 $s$、$M$ 和 $T$ 分别为专家的内在秩、模型数量和轮数——从而避免了维度灾难。

Finally, we also provide a computationally efficient and parameter-free implementation of HPG.

最后，我们还提供了一种计算高效且无需参数设置的 HPG 实现方案。