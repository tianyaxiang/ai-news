---
title: "Recursive CTEs: SQL’s Hidden Graph Traversal Engine"
originalUrl: "https://towardsdatascience.com/six-degrees-of-sql/"
date: "2026-08-25T22:02:12.507Z"
---

# Recursive CTEs: SQL’s Hidden Graph Traversal Engine
# 递归 CTE：SQL 中隐藏的图遍历引擎

When developers run into graph problems, such as working with hierarchies, finding routes between cities, or mapping social network connections, the first reaction is often to grab a specialised tool. We tend to think we need a graph database like Neo4j or a Python library like NetworkX to handle these challenges.
当开发者遇到图论问题时，例如处理层级结构、寻找城市间的路线或映射社交网络连接，第一反应往往是寻找专门的工具。我们倾向于认为需要像 Neo4j 这样的图数据库或像 NetworkX 这样的 Python 库来应对这些挑战。

For massive, billion-node graphs, those tools are necessary. But for a substantial percentage of operational data, e.g. supply chains with a few thousand nodes, organisational charts, or navigation paths, introducing a new database engine can be architectural overkill.
对于拥有数十亿节点的超大规模图，这些工具是必要的。但对于相当大比例的业务数据，例如拥有几千个节点的供应链、组织架构图或导航路径，引入一个新的数据库引擎往往会导致架构过度设计。

Your current relational database can likely already handle most graph problems just fine. The key is a feature that has been in the SQL standard since 1999 but still isn't widely used: Recursive Common Table Expressions (CTEs).
你现有的关系型数据库很可能已经能够很好地处理大多数图论问题。关键在于一项自 1999 年起就已纳入 SQL 标准，但至今仍未被广泛使用的功能：递归公用表表达式（Recursive CTEs）。

This article shows how to do graph traversal, pathfinding, and cycle detection using only standard SQL.
本文将展示如何仅使用标准 SQL 来实现图遍历、路径查找和循环检测。

### Prerequisites
### 前置条件

If you want to try these examples, you’ll need access to a modern relational database like Postgres, Oracle, or MySQL. I’ll use SQLite on my local computer for the examples. Keep in mind that the exact SQL syntax may vary slightly depending on your database.
如果你想尝试这些示例，你需要访问一个现代关系型数据库，如 Postgres、Oracle 或 MySQL。我将在本地计算机上使用 SQLite 进行演示。请记住，具体的 SQL 语法可能会根据你所使用的数据库而略有不同。

### A Quick Refresher on Standard CTEs
### 标准 CTE 快速回顾

Before diving into recursive common table expressions, let’s review what a regular, non-recursive CTE does.
在深入探讨递归公用表表达式之前，让我们先回顾一下常规的非递归 CTE 的作用。

A Common Table Expression is a temporary result set you define within a single SELECT, INSERT, UPDATE, or DELETE statement. You can think of it as a named subquery or a temporary view that only exists while the query runs.
公用表表达式（CTE）是你可以在单个 SELECT、INSERT、UPDATE 或 DELETE 语句中定义的临时结果集。你可以将其视为一个命名的子查询或仅在查询运行期间存在的临时视图。

The main reason to use CTEs is to make queries more straightforward to read. They help you break up complex logic into clear, step-by-step parts. Instead of writing messy queries full of subqueries, you can organise your logic in named sections at the top. For complex SQL, you can also reuse the same CTE more than once in a statement.
使用 CTE 的主要原因是使查询更易于阅读。它们有助于将复杂的逻辑分解为清晰的、分步骤的部分。与其编写充满子查询的混乱语句，不如将逻辑组织在顶部的命名部分中。对于复杂的 SQL，你还可以在同一个语句中多次重用同一个 CTE。

Consider a simple sales table.
考虑一个简单的销售表。

```sql
CREATE TABLE sales (id SERIAL PRIMARY KEY, region TEXT, amount INT);
INSERT INTO sales (region, amount) VALUES ('North', 100);
INSERT INTO sales (region, amount) VALUES ('North', 150);
INSERT INTO sales (region, amount) VALUES ('South', 200);
INSERT INTO sales (region, amount) VALUES ('South', 50);
INSERT INTO sales (region, amount) VALUES ('East', 300);
```

We want to find out which region(s) sales are greater than or equal to the average sales of all regions. First, we use a CTE to calculate total sales for each region. Then, we filter these results by comparing each region’s total to the average, which we get from a subquery over the CTE.
我们想找出哪些地区的销售额大于或等于所有地区的平均销售额。首先，我们使用 CTE 计算每个地区的总销售额。然后，我们将每个地区的总额与平均值进行比较来过滤这些结果，平均值通过对 CTE 进行子查询获得。

```sql
WITH region_totals AS (
    -- Calculate total sales per region
    SELECT region, SUM(amount) as total_sales
    FROM sales
    GROUP BY region
)
SELECT region, total_sales
FROM region_totals
WHERE total_sales > (SELECT AVG(total_sales) FROM region_totals);

-- Output is ...
-- region  total_sales
-- ------  -----------
-- East    300
```

Let's check. Regional totals are: North = 250, South = 250, East = 300. The average of these values is 266.67, and yes, only the East region has sales greater than this average.
让我们核对一下。各地区总额为：北区 = 250，南区 = 250，东区 = 300。这些值的平均数是 266.67，没错，只有东区的销售额大于此平均值。

The `WITH region_totals AS (…)` block defines the CTE. The subsequent query treats `region_totals` like it's a real table. Once the query finishes, the CTE vanishes.
`WITH region_totals AS (…)` 代码块定义了 CTE。随后的查询将 `region_totals` 视为一张真实的表。一旦查询结束，CTE 就会消失。

A Recursive CTE takes this concept one step further. Instead of just passing data down to the main query, it can reference itself to generate new rows based on previous rows.
递归 CTE 将这一概念更进一步。它不仅可以将数据传递给主查询，还可以引用自身，从而基于前几行数据生成新的行。

### The Anatomy of a Recursive CTE
### 递归 CTE 的剖析

A Recursive CTE works like a loop inside a query. Unlike a regular SELECT statement that runs once, a recursive CTE keeps running until there’s no more data. It builds the result set step by step.
递归 CTE 的工作方式类似于查询内部的循环。与运行一次的常规 SELECT 语句不同，递归 CTE 会持续运行，直到没有更多数据为止。它一步步构建结果集。

The syntax is standardised, but the logic requires a mental shift from "set-based" thinking to "iterative" thinking.
其语法是标准化的，但逻辑需要从“基于集合”的思维转向“迭代”思维。

Every recursive CTE has the same basic structure: an initial (or anchor) query to start the result set, a recursive query that joins back to itself to add more rows, and a stopping point when there are no more rows to add.
每个递归 CTE 都具有相同的基本结构：一个用于启动结果集的初始（或锚点）查询，一个通过连接自身来添加更多行的递归查询，以及当没有更多行可添加时的停止点。

For example:
例如：

```sql
WITH RECURSIVE graph_cte AS (
    -- 1. Initial query to start the result set (The Anchor)
    SELECT * FROM table WHERE id = 1
    UNION ALL
    -- 2. Recursive query that joins back to itself to add more rows
    SELECT t.* FROM table t
    JOIN graph_cte g ON t.parent_id = g.id
    -- 3. Stopping point: The recursion terminates automatically when this JOIN
    -- finds no more matches and returns 0 rows.
)
SELECT * FROM graph_cte;
```

When you run this, the database engine does a Breadth-First Search (BFS). It runs the anchor query, adds the results to a working table, then uses those results for the recursive step. This repeats until the recursive step returns no more rows.
当你运行此代码时，数据库引擎会执行广度优先搜索（BFS）。它运行锚点查询，将结果添加到工作表中，然后将这些结果用于递归步骤。此过程会重复进行，直到递归步骤不再返回任何行。

### Example 1. The Organisational Hierarchy (Trees)
### 示例 1：组织层级（树结构）

The most common graph problem in business software is the tree structure. File systems, comment threads, and organisational charts all model hierarchical relationships, even though the meaning of those relationships differs in each case.
商业软件中最常见的图论问题是树结构。文件系统、评论线程和组织架构图都模拟了层级关系，尽管这些关系在每种情况下的含义各不相同。

Let's define a simple employees table.
让我们定义一个简单的员工表。

```sql
CREATE TABLE employees (
    id SERIAL PRIMARY KEY,
    name TEXT NOT NULL,
    manager_id INT REFERENCES employees(id),
    role TEXT
);

INSERT INTO employees (id, name, manager_id, role) VALUES
(1, 'Alice', NULL, 'CEO'),
(2, 'Bob', 1, 'VP Engineering'),
(3, 'Charlie', 1, 'VP Sales'),
(4, 'Dave', 2, 'Backend Lead'),
(5, 'Eve', 2, 'Frontend Lead'),
(6, 'Frank', 4, 'Junior Dev');
```

The problem: We want to create a report that shows every employee, their management path (e.g., “Alice -> Bob -> Dave”), and their depth in the hierarchy.
问题：我们想要创建一个报告，显示每位员工、他们的管理路径（例如，“Alice -> Bob -> Dave”）以及他们在层级中的深度。

The SQL Solution:
SQL 解决方案：

```sql
WITH RECURSIVE org_chart AS (
    -- Anchor: Start with the Boss (Depth 1)
    SELECT id, name, manager_id, role, 1 AS depth, name AS path
    FROM employees
    WHERE manager_id IS NULL
    UNION ALL
    -- Recursive: Find employees managed by the previous layer
    SELECT e.id, e.name, e.manager_id, e.role, oc.depth + 1 AS depth,
           oc.path || ' -> ' || e.name AS path
    FROM employees e
    JOIN org_chart oc ON e.manager_id = oc.id
)
SELECT * FROM org_chart;
```