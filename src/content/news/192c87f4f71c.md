---
title: "RADIO-ViPE: Online Tightly Coupled Multi-Modal Fusion for Open-Vocabulary Semantic SLAM in Dynamic Environments"
originalUrl: "https://arxiv.org/abs/2604.26067"
date: "2026-04-30T22:47:34.536Z"
---

# RADIO-ViPE: Online Tightly Coupled Multi-Modal Fusion for Open-Vocabulary Semantic SLAM in Dynamic Environments
# RADIO-ViPE：动态环境中用于开放词汇语义 SLAM 的在线紧耦合多模态融合系统

**Abstract:** We present RADIO-ViPE (Reduce All Domains Into One -- Video Pose Engine), an online semantic SLAM system that enables geometry-aware open-vocabulary grounding, associating arbitrary natural language queries with localized 3D regions and objects in dynamic environments.

**摘要：** 我们提出了 RADIO-ViPE（Reduce All Domains Into One -- Video Pose Engine），这是一个在线语义 SLAM 系统，能够实现具备几何感知能力的开放词汇定位（grounding），将任意自然语言查询与动态环境中的局部 3D 区域和物体相关联。

Unlike existing approaches that require calibrated, posed RGB-D input, RADIO-ViPE operates directly on raw monocular RGB video streams, requiring no prior camera intrinsics, depth sensors, or pose initialization.

与需要经过校准、具备位姿信息的 RGB-D 输入的现有方法不同，RADIO-ViPE 直接在原始单目 RGB 视频流上运行，无需预先获取相机内参、深度传感器数据或进行位姿初始化。

The system tightly couples multi-modal embeddings -- spanning vision and language -- derived from agglomerative foundation models (e.g., RADIO) with geometric scene information. This coupling takes place in initialization, optimization and factor graph connections to improve the consistency of the map from multiple modalities.

该系统将源自聚合基础模型（如 RADIO）的多模态嵌入（涵盖视觉和语言）与几何场景信息进行了紧密耦合。这种耦合发生在初始化、优化和因子图连接过程中，旨在提高多模态地图的一致性。

The optimization is wrapped within adaptive robust kernels, designed to handle both actively moving objects and agent-displaced scene elements (e.g., furniture rearranged during ego-centric session).

优化过程封装在自适应鲁棒核函数中，旨在处理主动移动的物体以及被智能体位移的场景元素（例如，在以自我为中心的拍摄过程中被重新摆放的家具）。

Experiments demonstrate that RADIO-ViPE achieves state-of-the-art results on the dynamic TUM-RGBD benchmark while maintaining competitive performance against offline open-vocabulary methods that rely on calibrated data and static scene assumptions.

实验表明，RADIO-ViPE 在动态 TUM-RGBD 基准测试中达到了最先进（SOTA）的水平，同时在与依赖校准数据和静态场景假设的离线开放词汇方法相比时，依然保持了极具竞争力的性能。

RADIO-ViPE bridges a critical gap in real-world deployment, enabling robust open-vocabulary semantic grounding for autonomous robotics and unconstrained in-the-wild video streams.

RADIO-ViPE 弥补了现实世界部署中的一个关键差距，为自主机器人和不受约束的野外视频流提供了稳健的开放词汇语义定位能力。