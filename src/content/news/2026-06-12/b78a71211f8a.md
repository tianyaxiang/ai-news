---
title: "PySpark for Beginners: Beyond the Basics"
originalUrl: "https://towardsdatascience.com/pyspark-for-beginners-beyond-the-basics/"
date: "2026-06-11T23:11:31.593Z"
---

# PySpark for Beginners: Beyond the Basics
# PySpark 初学者指南：进阶篇

**Take the next step to building real workflows with Spark on your laptop**
**在笔记本电脑上迈出构建真实 Spark 工作流的下一步**

Thomas Reid | Jun 11, 2026 | 12 min read

If you’ve read my first article in this series, *PySpark for Beginners: Mastering the Basics*, then you already understand the heart of Spark: distributed data, DataFrames, and lazy execution. You’ve installed PySpark, stood up a SparkSession, read a CSV and performed simple manipulations of the data in a Dataframe. I’ll leave a link to that story at the end of this one.
如果你读过我这个系列的第一篇文章《PySpark 初学者指南：掌握基础》，那么你已经理解了 Spark 的核心：分布式数据、DataFrame 和惰性执行。你已经安装了 PySpark，启动了 SparkSession，读取了 CSV 文件，并对 DataFrame 中的数据进行了简单的操作。我会在本文末尾附上那篇文章的链接。

One thing worth repeating from that original article is that I often use the terms PySpark and Spark interchangeably, but strictly speaking, Spark is the overarching distributed computing framework (written in Scala), and PySpark is a dedicated Python API to Spark.
有一点值得从那篇文章中重申：我经常交替使用 PySpark 和 Spark 这两个术语，但严格来说，Spark 是总体的分布式计算框架（用 Scala 编写），而 PySpark 是专门用于 Spark 的 Python API。

### Beyond the basics
### 进阶之路

Now, something interesting happens when you get past that beginner stage. You quickly realise your second PySpark project requires a slightly different mindset: You want to read/write data in a safer, faster, more predictable way. You want to combine datasets without feeling uncertain about joins. You want to understand why Spark is behaving the way it does — and how to nudge it gently in the right direction.
当你跨过初学者阶段后，有趣的事情发生了。你很快会意识到，你的第二个 PySpark 项目需要一种略有不同的思维方式：你希望以更安全、更快、更可预测的方式读写数据；你希望在合并数据集时不再对连接（join）操作感到困惑；你希望理解 Spark 为什么会这样运行，以及如何引导它向正确的方向优化。

This article takes you through those next steps. It’s deliberately slow‑paced and practical. No deep internals. No cluster tuning. No complicated Spark optimisations. Just the things real beginners need to know when they move from toy examples to small, real-world work. We’re using open‑source Spark, running locally, just like before.
本文将带你完成这些后续步骤。文章节奏刻意放慢且注重实践。没有深层的内部原理，没有集群调优，也没有复杂的 Spark 优化。只有初学者在从“玩具示例”转向小型实际工作时真正需要了解的内容。我们依然像之前一样，在本地运行开源版 Spark。

---

### 1. Taking the next step: reading data properly
### 1. 迈出下一步：正确地读取数据

In my first article, we used the simplest possible CSV loader:
在我的第一篇文章中，我们使用了最简单的 CSV 加载方式：

```python
df = spark.read.csv("sales.csv", header=True, inferSchema=True)
```

It works - and it’s fine for early experiments - but it hides a subtle problem. Spark is guessing your data types.
它确实有效，对于早期的实验来说也足够了，但它隐藏了一个微妙的问题：Spark 正在猜测你的数据类型。

When you use the `inferSchema=True` directive, Spark looks at a small sample of your file and uses that information to guess whether a column is an integer, string, boolean, or double. That means:
当你使用 `inferSchema=True` 指令时，Spark 会查看文件的一小部分样本，并利用这些信息来猜测某列是整数、字符串、布尔值还是双精度浮点数。这意味着：

*   If 99 rows appear to be numeric and the 100th row is blank, Spark might interpret the column as a string.
    如果 99 行看起来是数字，而第 100 行是空白，Spark 可能会将该列解释为字符串。
*   If someone edits the file next week and accidentally adds £23.50 instead of 23.50, Spark might treat the entire column differently.
    如果下周有人编辑文件时不小心输入了 £23.50 而不是 23.50，Spark 可能会以完全不同的方式处理整列数据。
*   If your file is large, the sample Spark uses won’t represent the whole dataset. This can lead to mysterious behaviour later, the kind of bugs beginners find hardest to diagnose.
    如果你的文件很大，Spark 使用的样本将无法代表整个数据集。这可能导致后续出现难以捉摸的行为，这也是初学者最难排查的那类 Bug。

**A better beginner habit: define a schema for your data**
**更好的初学者习惯：为数据定义 Schema（模式）**

Think of a schema as Spark’s version of a blueprint for reading data. Before building anything, you tell Spark things like:
可以将 Schema 想象成 Spark 读取数据的“蓝图”。在构建任何东西之前，你需要告诉 Spark 以下信息：

*   The names of the columns
    列名
*   What data type they should be
    它们应该是什么数据类型
*   Whether or not a column value is optional.
    列值是否可选。

Here’s what it looks like for our sales data example. Recall that the data looked like this:
以下是我们销售数据示例的实现方式。回想一下，数据看起来是这样的：

```text
transaction_id,customer_name,net_amount,tax_amount,is_member
101,Alice,250.50,25.05,true
102,Bob,120.00,6.00,false
103,Charlie,450.75,25.07,true
104,David,89.99,5.73,false
```

To specify the types of the above fields in Spark, we define our schema using code like the following:
为了在 Spark 中指定上述字段的类型，我们使用如下代码定义 Schema：

```python
from pyspark.sql import types as T

schema = T.StructType([
    T.StructField("transaction_id", T.IntegerType(), False),
    T.StructField("customer_name", T.StringType(), False),
    T.StructField("net_amount", T.DoubleType(), True),
    T.StructField("tax_amount", T.DoubleType(), True),
    T.StructField("is_member", T.BooleanType(), True),
])
```

The column names and type parameters are self-explanatory. The `True/False` parameter indicates that there may [not] be NULL values in the column. Note, the `True/False` nullability flag is mostly schema metadata and optimisation info. It’s not always strictly enforced for every data source the way a database `NOT NULL` constraint is.
列名和类型参数是不言自明的。`True/False` 参数表示该列中可能 [或不可能] 存在 NULL 值。请注意，这个可空性标志主要用于 Schema 元数据和优化信息，它并不总是像数据库中的 `NOT NULL` 约束那样对每个数据源都严格执行。

**More useful options when reading CSV data**
**读取 CSV 数据时更有用的选项**

There’s a bunch of handy CSV read options you can combine with the schema directive that make loading CSV data even more reliable. The more common options include:
你可以将许多实用的 CSV 读取选项与 Schema 指令结合使用，使加载 CSV 数据更加可靠。常见的选项包括：

*   `mode=”PERMISSIVE”`: keeps bad rows as much as possible
    `mode=”PERMISSIVE”`：尽可能保留错误行
*   `mode=”DROPMALFORMED”`: drops malformed rows
    `mode=”DROPMALFORMED”`：丢弃格式错误的行
*   `mode=”FAILFAST”`: errors immediately
    `mode=”FAILFAST”`：立即报错
*   `header=True/False`: Does the file contain [or not] a header record
    `header=True/False`：文件是否包含标题行
*   `nullValue`: what text should replace null values in the input
    `nullValue`：输入中应将什么文本替换为 null 值
*   `dateFormat / timestampFormat`
    `dateFormat / timestampFormat`（日期/时间戳格式）

Now we can load the sales_data into a Dataframe like this:
现在我们可以像这样将 sales_data 加载到 DataFrame 中：

```python
df = (
    spark.read
    .option("header", True)
    .option("mode", "FAILFAST")
    .option("nullValue", "N/A")
    .schema(schema)
    .csv("sales_data.csv")
)
```

**Why is this important for beginners?**
**这对初学者为什么很重要？**

*   You know what the data types are before you start working.
    在开始工作之前，你就明确了数据类型。
*   If specified, Spark will reject weird rows instead of silently interpreting them.
    如果指定了 Schema，Spark 会拒绝异常行，而不是默默地进行错误解释。
*   Your transformations become more predictable.
    你的转换操作变得更加可预测。
*   If you join two datasets later, type mismatches won’t surprise you.
    如果你稍后连接两个数据集，类型不匹配的问题就不会让你措手不及。

---

### 2. Understanding data transformations
### 2. 理解数据转换

Recall, in my previous article, in our first steps with manipulating dataframes with PySpark, we added an extra, derived column to our Dataframe using code like this:
回想一下，在上一篇文章中，我们在使用 PySpark 操作 DataFrame 的第一步中，通过以下代码向 DataFrame 添加了一个额外的派生列：

```python
df2 = df.withColumn("gross_amount", df.net_amount + df.tax_amount)
```

I explained that this line doesn’t calculate anything yet. It simply adds a step to Spark’s internal plan:
我解释过，这一行代码还没有进行任何计算。它只是向 Spark 的内部计划中添加了一个步骤：

1. Read the CSV (读取 CSV)
2. Add a new column (gross_amount = net + tax) (添加新列)

Then you might add more steps like this:
然后你可能会添加更多步骤，例如：

```python
df3 = df2.withColumn("tax_percentage", df2.tax_amount / df2.gross_amount * 100)
```

Still, no computation has happened. Only when you perform an action like `df3.show()` does Spark say: “Okay, now I need to actually run all these steps.”
此时，依然没有发生任何计算。只有当你执行像 `df3.show()` 这样的动作时，Spark 才会说：“好的，现在我需要真正运行所有这些步骤了。”

This is what “lazy execution” means, but the important bit for beginners isn’t the name. It’s the effect, and it means:
这就是“惰性执行”的含义，但对初学者来说，重要的不是这个名字，而是它的效果。这意味着：

*   You can chain many transformations without “paying” for them until you need the result.
    你可以串联许多转换操作，而无需在需要结果之前“付出”计算代价。
*   Spark can rearrange the order internally to run things efficiently.
    Spark 可以在内部重新排列顺序，以高效地运行任务。
*   You don’t waste time doing intermediate steps on data you might filter out later.
    你不会浪费时间在那些稍后可能会被过滤掉的数据上执行中间步骤。

Think of it like you would an everyday task, like making a sandwich: You gather all the ingredients...
把它想象成你每天做的任务，比如做三明治：你先收集所有的配料……