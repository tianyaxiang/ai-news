---
title: "3 Agents. 3 LLMs. 1 Aging GPU: Engineering Parallel Inference on Bare Metal"
originalUrl: "https://towardsdatascience.com/3-agents-3-llms-1-aging-gpu-engineering-parallel-inference-on-bare-metal/"
date: "2026-06-25T23:04:17.058Z"
---

# 3 Agents. 3 LLMs. 1 Aging GPU: Engineering Parallel Inference on Bare Metal
# 3个智能体、3个大模型、1块老旧显卡：在裸机上实现并行推理工程

How a tiny C++ daemon uses 5G-style admission control and asynchronous layer pipelining to run three LLMs on an 8-year-old graphics card.
一个微小的 C++ 守护进程如何利用 5G 风格的准入控制和异步层流水线技术，在 8 年前的显卡上运行三个大模型。

Anubhab Banerjee | Jun 25, 2026 | 21 min read
Anubhab Banerjee | 2026年6月25日 | 21分钟阅读

The VRAM Conductor architecture: combining 5G-style connection admission control (lmxd) with asynchronous double-buffered layer streaming to safely multiplex multiple LLMs on a single 8GB GTX 1080. Image generated using Claude 4.7 Opus.
显存调度器（VRAM Conductor）架构：结合了 5G 风格的连接准入控制（lmxd）与异步双缓冲层流式传输，从而在单块 8GB GTX 1080 显卡上安全地多路复用多个大模型。图片由 Claude 4.7 Opus 生成。

You have three AI agents using three different LLMs. You have one ancient GPU and you are too poor to upgrade. You need to run these agents in parallel, but on this old GPU, exactly one survives and the other two crash. Here is the small C++ daemon that fixes that, and the true story of how they all survive together.
你有三个使用不同大模型的 AI 智能体。你只有一块古老的 GPU，而且穷得买不起升级版。你需要并行运行这些智能体，但在旧显卡上，往往只有一个能存活，另外两个会崩溃。这里有一个能解决该问题的微型 C++ 守护进程，以及它们如何共同存活的真实故事。

### The problem you actually have
### 你真正面临的问题

Let me describe a situation with which you can relate very easily. You have a few AI agents: Agent A generates the raw code, Agent B actively reviews it for security flaws as it is being written, and Agent C simultaneously drafts the documentation. To achieve a seamless, real-time developer experience without massive lag spikes, all three must be resident in memory at the same time. They each work best with a different small instruct LLM — a SmolLM here, a Qwen there, a small Llama somewhere else.
让我描述一个你很容易产生共鸣的情况。你有几个 AI 智能体：智能体 A 生成原始代码，智能体 B 在代码编写时主动审查安全漏洞，智能体 C 同时起草文档。为了实现无缝、实时的开发体验且不出现严重的延迟卡顿，这三个智能体必须同时驻留在内存中。它们各自最适合使用不同的小型指令微调大模型——这里用 SmolLM，那里用 Qwen，别处再用个小型的 Llama。

You point them at your machine, which has one very old GPU. You can’t upgrade it this quarter, or this year, or possibly this lifetime (yes, you are that poor!). It is an NVIDIA GTX 1080 with only 8 GB of VRAM, and you have spent years quietly being told that it should still be enough for “small” models. So you do the obvious thing. You open three terminals to launch these agents in parallel via three llama-completion processes. And then you wait to see this:
你把它们指向你的机器，上面只有一块非常老旧的 GPU。你这个季度、今年，甚至这辈子都无法升级它（是的，你就是这么穷！）。这是一块只有 8GB 显存的 NVIDIA GTX 1080，多年来你一直被告知它对于“小型”模型来说应该足够了。于是你做了显而易见的事：打开三个终端，通过三个 `llama-completion` 进程并行启动这些智能体。然后你等着看这一幕：

You: “OK, even though the GPU is old, three small models should be working fine.”
你：“好吧，虽然显卡老了点，但三个小模型应该能正常运行。”

llama-completion (Agent 1, Llama 3.2 1B): “Loading backend. Reserving KV cache up front for n_ctx=172032, n_batch=8192, -ngl 99.”
llama-completion (智能体 1, Llama 3.2 1B)：“正在加载后端。预留 KV 缓存，n_ctx=172032, n_batch=8192, -ngl 99。”

GPU memory jumps to 6,536 MiB out of 8,192.
GPU 显存占用从 8,192 MiB 跳升至 6,536 MiB。

You: “Cool. Now Agent 2 — Qwen2 0.5B.”
你：“酷。现在启动智能体 2 —— Qwen2 0.5B。”

llama-completion (Agent 2): “Allocating 1,536 MiB on device 0…” cudaMalloc: “❌ out of memory.”
llama-completion (智能体 2)：“正在设备 0 上分配 1,536 MiB……” cudaMalloc：“❌ 内存不足。”

llama-completion (Agent 2): “qwen process ended.” 🫡
llama-completion (智能体 2)：“Qwen 进程已结束。” 🫡

You: “Fine. SmolLM2 360M, then. That one is tiny.”
你：“好吧。那就 SmolLM2 360M。那个很小。”

llama-completion (Agent 3): “Allocating 5,120 MiB on device 0…” cudaMalloc: “❌ out of memory.”
llama-completion (智能体 3)：“正在设备 0 上分配 5,120 MiB……” cudaMalloc：“❌ 内存不足。”

llama-completion (Agent 3): “smol process ended.”
llama-completion (智能体 3)：“Smol 进程已结束。”

Your nvidia-smi: still shows only one process at 6,512 MiB. Your three-agent demo: is in fact a one-agent demo with two crash logs. If this looks familiar, you are not doing anything wrong. You are doing exactly what every “multi-agent on a single GPU” tutorial tells you to do. The problem is that the tutorial is being optimistic about the silicon.
你的 `nvidia-smi`：仍然只显示一个占用 6,512 MiB 的进程。你的三智能体演示：实际上是一个带两个崩溃日志的单智能体演示。如果这看起来很熟悉，说明你没做错什么。你完全按照每一个“单 GPU 多智能体”教程所说的去做。问题在于，这些教程对硬件性能过于乐观了。

### Why the 3 LLMs can’t run in parallel (the one-minute version)
### 为什么 3 个大模型无法并行运行（一分钟简述）

llama.cpp‘s llama-completion (and friends) reserve the KV cache (per-token memory the attention layers use during decoding) for the full configured context window, up front, when the llama_context is created. Yes, it is up front, not as you go type, to ensure the decoding process runs smoothly without any hiccups. With -c 172032 and -ngl 99, the first process happily eats 6,536 MiB / 8,192 MiB of your card before it has decoded a single token, and almost none of that is the model weights. It is the KV reservation.
`llama.cpp` 的 `llama-completion`（及其相关程序）在创建 `llama_context` 时，会预先为完整的配置上下文窗口预留 KV 缓存（解码过程中注意力层使用的每个 token 的内存）。是的，是预先分配，而不是按需分配，以确保解码过程平稳运行，不会出现任何卡顿。使用 `-c 172032` 和 `-ngl 99` 参数时，第一个进程在解码出第一个 token 之前，就愉快地占用了你显卡 8,192 MiB 中的 6,536 MiB，而其中几乎没有模型权重，全是 KV 预留空间。

At that time, when the second process tries to build its own context and gets exactly this in its log:
此时，当第二个进程尝试构建自己的上下文时，日志中会出现以下内容：

`0.00.592.688 E ggml_backend_cuda_buffer_type_alloc_buffer: allocating 1536.00 MiB on device 0: cudaMalloc failed: out of memory`
`0.00.593.132 E llama_init_from_model: failed to initialize the context: failed to allocate buffer for kv cache`

This isn’t a bug. It is llama.cpp doing the safe thing for one process: pre-reserve KV so decoding never stalls mid-stream. It happens to be a deeply unsafe thing when three independent processes do it on the same 8 GB card, because the card has no queue and there is no shared accounting. There is only cudaMalloc which becomes literally a coin flip the moment the card is past ~80 % full.
这不是 Bug。这是 `llama.cpp` 为单个进程所做的安全措施：预留 KV 以确保解码过程中不会中断。但当三个独立进程在同一块 8GB 显卡上这样做时，情况就变得极其不安全了，因为显卡没有队列机制，也没有共享记账功能。只有 `cudaMalloc`，而一旦显卡占用超过 80%，它就完全变成了掷硬币式的运气游戏。

The fix is not “a better algorithm.” It is the simplest of all: bookkeeping. Someone has to look at the card, decide whether the next agent will actually fit, and refuse the request before the next process tries to allocate.
解决方法不是“更好的算法”，而是最简单的：记账。必须有人监控显卡，判断下一个智能体是否真的能装下，并在下一个进程尝试分配内存之前拒绝该请求。

### The solution: a small C++ daemon that does the bookkeeping
### 解决方案：一个负责记账的微型 C++ 守护进程

We needed a bookkeeper, so let’s build that bookkeeper. lmxd is one long-lived process (about 1,500 lines of C++17 only) that owns the GPU on behalf of the agents. Agents do not spawn their own llama-completion binaries anymore, they just talk to the daemon over a tiny Unix-socket text protocol (HELP, STATUS, LIST, REGISTER, UNREGISTER), and the daemon decides whether the new agent fits, and only then loads the model.
我们需要一个记账员，那就来构建它。`lmxd` 是一个长驻进程（仅约 1,500 行 C++17 代码），它代表智能体拥有 GPU 的控制权。智能体不再自行启动 `llama-completion` 二进制文件，而是通过一个微小的 Unix 套接字文本协议（HELP, STATUS, LIST, REGISTER, UNREGISTER）与守护进程通信。守护进程决定新智能体是否能装下，只有在确认后才会加载模型。

The whole policy lives in one number: 90 % of the card’s total VRAM, and one rule: admit a new agent only if `currently_used + new_estimate ≤ 90 % cap`. Everything below is just an honest implementation of that rule.
整个策略基于一个数字：显卡总显存的 90%，以及一条规则：仅当 `当前已用 + 新预估值 ≤ 90% 上限` 时才准入新智能体。以下所有内容都是对该规则的忠实实现。

The ledger that enforces the cap is small enough to read in one sitting. From `src/vram_ledger.cpp`:
执行该上限的账本非常小，可以一次读完。摘自 `src/vram_ledger.cpp`：

```cpp
bool VramLedger::try_reserve(uint64_t model_table_bytes) {
    // Single critical section covers compare-and-bump so parallel REGISTERs cannot overcommit.
    std::lock_guard<std::mutex> lock(mu_);
    if (!initialized_) {
        return false;
    }
    uint64_t projected = 0;
    if (!add_u64(allocated_bytes_, model_table_bytes, &projected)) {
        return false;
    }
    // ...
```