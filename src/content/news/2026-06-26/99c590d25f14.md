---
title: "Curvature-Guided Mixing for MLLM Adaptation"
originalUrl: "https://arxiv.org/abs/2606.24963"
date: "2026-06-25T23:03:13.845Z"
---

# Curvature-Guided Mixing for MLLM Adaptation
# 曲率引导的混合：多模态大语言模型（MLLM）适配新方法

**Abstract:** Fine-tuning Multimodal Large Language Models (MLLMs) on specialized tasks often leads to catastrophic forgetting of their general capabilities. Existing model merging methods to combat this are often heuristic or use sub-optimal objectives. 

**摘要：** 在特定任务上对多模态大语言模型（MLLM）进行微调，往往会导致模型对其通用能力的灾难性遗忘。现有的用于解决此问题的模型合并方法通常基于启发式规则，或使用次优的目标函数。

We propose Curvature-Guided Mixing (CGM), a theoretically grounded framework that merges pre-trained and fine-tuned models. CGM formulates a joint optimization objective and uses a second-order (Hessian) approximation of the loss landscapes to analytically derive an optimal, closed-form "soft mixing" ratio. This ratio intelligently blends parameters based on their relative task-specific curvatures. 

我们提出了“曲率引导混合”（Curvature-Guided Mixing, CGM），这是一个具有理论基础的框架，用于合并预训练模型和微调模型。CGM 构建了一个联合优化目标，并利用损失平面的二阶（Hessian）近似，解析推导出一种最优的闭式“软混合”比例。该比例根据参数在特定任务中的相对曲率，智能地对参数进行融合。

We also introduce CGM$\dagger$, a robust "hard mixing" variant that performs sparse parameter selection guided by a novel, curvature-aware score. Experiments on LLaVA-1.5 and Qwen2.5VL across multiple downstream tasks show that CGM and CGM$\dagger$ consistently improve the trade-off between task specialization and general knowledge retention over existing methods. Code is available at this http URL.

我们还引入了 CGM$\dagger$，这是一种稳健的“硬混合”变体，它通过一种新颖的曲率感知评分来执行稀疏参数选择。在 LLaVA-1.5 和 Qwen2.5VL 上针对多个下游任务的实验表明，与现有方法相比，CGM 和 CGM$\dagger$ 在任务专业化与通用知识保留之间的权衡上表现出持续的改进。代码已在相关链接中提供。