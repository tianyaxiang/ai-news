---
title: "Single-Query Person-Centric Bimanual Hand-Object Interaction Detection"
originalUrl: "https://arxiv.org/abs/2609.12155"
date: "2026-09-15T00:18:27.902Z"
---

# Single-Query Person-Centric Bimanual Hand-Object Interaction Detection
# 单查询以人为中心的双手与物体交互检测

**Abstract:** Understanding person-level bi-manual interactions requires not only detecting hands, but also identifying which two hands belong to the same person and what each hand interacts with.
**摘要：** 理解人级别的双手交互不仅需要检测手部，还需要识别哪两只手属于同一个人，以及每只手正在与什么物体进行交互。

Existing hand--object interaction methods are mostly hand-centric: they treat each hand as an independent instance, which can lead to ambiguous ownership in multi-person scenes.
现有的手-物交互方法大多以手为中心：它们将每只手视为一个独立的实例，这在多人场景中可能导致归属关系模糊。

We propose a person-centric formulation in which a single query predicts a structured output for one person, including the human box, body pose, hand boxes and states, and interaction targets.
我们提出了一种以人为中心的公式化方法，其中单个查询（query）可以预测一个人的结构化输出，包括人体框、身体姿态、手部框和状态，以及交互目标。

We introduce part-aware deformable attention to allocate attention across human, hand, and pose-specific reference regions, enabling one query to capture the full person structure.
我们引入了部件感知可变形注意力机制（part-aware deformable attention），在人体、手部和姿态特定的参考区域之间分配注意力，使单个查询能够捕捉到完整的人体结构。

We further unify detection and interaction reasoning with a hand-to-query relationship matrix, where each hand selects its interaction target from the detected query set plus a learnable off token, directly recovering the target's box and class without separate object regression.
我们进一步通过手-查询关系矩阵统一了检测和交互推理，每只手从检测到的查询集加上一个可学习的“无交互（off）”标记中选择其交互目标，直接恢复目标的框和类别，而无需单独的物体回归。

We build a COCO-based dataset with person-centric bi-manual interaction annotations and define structured metrics for evaluating hand states and complete hand--object tuples.
我们构建了一个基于 COCO 的数据集，包含以人为中心的双手交互标注，并定义了用于评估手部状态和完整手-物元组的结构化指标。

Experiments with a transformer-based detector show that our formulation improves person-level bi-manual interaction parsing and provides an effective unified framework for joint detection, pose estimation, and hand reasoning.
基于 Transformer 的检测器实验表明，我们的方法改进了人级别双手交互的解析，并为联合检测、姿态估计和手部推理提供了一个有效的统一框架。