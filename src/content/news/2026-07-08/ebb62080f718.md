---
title: "DELTAVID: Enhancing Fine-Grained Spatiotemporal Perception with Cross-Video Differences"
originalUrl: "https://arxiv.org/abs/2607.02551"
date: "2026-07-07T22:41:06.803Z"
---

# DELTAVID: Enhancing Fine-Grained Spatiotemporal Perception with Cross-Video Differences
# DELTAVID：通过跨视频差异增强细粒度时空感知

**Abstract:** Video multimodal large language models have made strong progress on open-ended video understanding, but they still lack precise local spatiotemporal perception. When two videos share almost the same global semantics and differ only in a short time span or a small region, current models often fail to find the change and provide reliable evidence.

**摘要：** 视频多模态大语言模型在开放式视频理解方面取得了显著进展，但它们仍然缺乏精确的局部时空感知能力。当两个视频具有几乎相同的全局语义，且仅在短时间跨度或小区域内存在差异时，现有模型往往无法发现这些变化并提供可靠的证据。

We propose DELTAVID, a verifiable proxy-task framework that enhances fine-grained spatiotemporal perception with cross-video differences. The key idea is to turn cross-video spot-the-difference into a trainable perception signal, where a model identifies local changes, judges temporal boundaries, and organizes spatial evidence by comparing similar videos.

我们提出了 DELTAVID，这是一个可验证的代理任务框架，旨在通过跨视频差异来增强细粒度时空感知。其核心思想是将跨视频的“找不同”任务转化为一种可训练的感知信号，使模型能够通过比较相似视频来识别局部变化、判断时间边界并组织空间证据。

To make this signal scalable to train and reliable to evaluate, we further introduce DELTAVID-10K and DELTAVID-Bench, which convert controllable local differences in real videos into evidence-labeled training and test samples.

为了使该信号具备可扩展的训练能力和可靠的评估标准，我们进一步引入了 DELTAVID-10K 和 DELTAVID-Bench，将真实视频中可控的局部差异转化为带有证据标签的训练和测试样本。

Experiments show that DELTAVID substantially improves performance on cross-video difference understanding and transfers the learned local evidence ability to general video understanding benchmarks, including MMVU, MLVU, Video-MME, VideoHolmes, VideoMMMU, LVBench, TempCompass, and LongVideoBench.

实验表明，DELTAVID 显著提升了跨视频差异理解的性能，并将所学的局部证据能力迁移到了通用的视频理解基准测试中，包括 MMVU、MLVU、Video-MME、VideoHolmes、VideoMMMU、LVBench、TempCompass 和 LongVideoBench。

These results show that cross-video differences are not only an effective way to diagnose fine-grained perception failures, but also a scalable proxy supervision that moves Video MLLMs from coarse semantic understanding toward fine-grained spatiotemporal evidence reasoning.

这些结果表明，跨视频差异不仅是诊断细粒度感知缺陷的有效途径，也是一种可扩展的代理监督方式，推动视频多模态大模型从粗粒度的语义理解向细粒度的时空证据推理迈进。