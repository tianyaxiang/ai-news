---
title: "Beyond extract_text: The Two Layers of a PDF That Drive RAG Quality"
originalUrl: "https://towardsdatascience.com/beyond-extract_text-the-two-layers-of-a-pdf-that-drive-rag-quality/"
date: "2026-06-10T23:09:47.975Z"
---

# Beyond extract_text: The Two Layers of a PDF That Drive RAG Quality
# 超越 extract_text：驱动 RAG 质量的 PDF 两层结构

Large Language Models Beyond extract_text: The Two Layers of a PDF That Drive RAG Quality Enterprise Document Intelligence [Vol.1 #5A] – Document signals (metadata, native TOC, source software) and page-level content (text vs scans, tables, images, columns, page profile) Kezhan Shi Jun 10, 2026 23 min read Share Photo by Romain Vignes, via Unsplash.
大型语言模型超越 extract_text：驱动 RAG 质量的 PDF 两层结构。企业文档智能 [第 1 卷 #5A] —— 文档信号（元数据、原生目录、源软件）与页面级内容（文本与扫描件、表格、图像、列、页面概况）。作者：Kezhan Shi，2026 年 6 月 10 日，阅读时长 23 分钟。图片来源：Romain Vignes (Unsplash)。

This article is the parsing brick of Enterprise Document Intelligence, a series that builds an enterprise RAG system from four bricks: parsing, question parsing, retrieval, and generation. Parsing comes first, and this is the first of its two parts. This part covers the first of parsing’s two layers: knowing the nature of the document (born-digital vs scanned, source software, declared metadata, native TOC) plus a short LLM-written summary of what it is. The next part turns the content into a relational set of tables.
本文是“企业文档智能”系列中关于“解析”这一模块的内容。该系列通过四个模块构建企业级 RAG 系统：解析、问题解析、检索和生成。解析是第一步，本文是该模块两部分中的第一部分。本部分涵盖了解析的第一层：了解文档的本质（原生数字文档与扫描件、源软件、声明的元数据、原生目录），以及由 LLM 撰写的简短文档摘要。下一部分将把内容转化为关系型表格集。

where this article sits in the series: Article 5, the nature-and-signals half of the parsing brick, inside Part II (the four bricks) – Image by author
本文在系列中的位置：第 5 篇文章，解析模块中关于“本质与信号”的部分，属于第二部分（四个模块）—— 图片由作者提供。

Before any retrieval happens in a RAG process, the parser has one job. Read the document the way a human would before answering a question about it. What is this thing? A CV, an insurance contract, a regulatory text, an academic paper? How many pages? Born digital or scanned, or stitched together from both? What does it carry: paragraphs, tables, multi-column layout, embedded images? In what language?
在 RAG 流程进行任何检索之前，解析器有一个核心任务：像人类在回答问题前那样阅读文档。这是什么？简历、保险合同、监管文本还是学术论文？有多少页？是原生数字文档、扫描件，还是两者拼接而成？它包含什么：段落、表格、多栏布局、嵌入图像？是什么语言？

Each of those checks is a failure case the rest of the pipeline cannot recover from:
上述每一项检查如果出错，都会导致后续流程无法挽回的失败：

A CV exported from a designer template. The candidate’s name sits in a logo image at the top of page 1, the rest of the page in clean text. Asked “what is the name?”, retrieval finds nothing matching and falls back to the PDF metadata’s author field, which is whoever last edited the file. The answer is wrong before generation ever runs.
一份从设计模板导出的简历。候选人姓名位于第一页顶部的 Logo 图片中，页面其余部分是纯文本。当被问及“名字是什么？”时，检索找不到匹配项，只能回退到 PDF 元数据的作者字段，即最后编辑该文件的人。在生成步骤运行之前，答案就已经错了。

An insurance contract with has_text_layer=True. The text layer is OCR output at quality 0.3. “Renewal fee EUR 250” comes through as “Renewa1 fee EUR 25O”. Keyword retrieval never matches; generation reads a different number nearby and commits to it.
一份 `has_text_layer=True` 的保险合同。其文本层是质量仅为 0.3 的 OCR 输出。“Renewal fee EUR 250”被识别为“Renewa1 fee EUR 25O”。关键词检索无法匹配；生成模型读取了附近的其他数字并将其作为答案。

A 200-page regulatory text with no TOC and no headings the parser can detect. The pipeline treats it as one homogeneous blob. The question parser has no idea page 4 holds the definitions and page 187 holds the exclusions.
一份 200 页的监管文本，没有目录，解析器也无法检测到标题。整个流程将其视为一个同质的块。问题解析器根本不知道第 4 页包含定义，而第 187 页包含排除条款。

An academic paper with two-column layout. Naive text extraction interleaves the left and right column line by line. The retrieved chunk reads as gibberish.
一份双栏布局的学术论文。简单的文本提取会逐行交错左右两栏的内容。检索到的片段读起来完全是乱码。

Same shape every time. An expert was asked a question about a document they had never opened. They guessed. The pipeline did the same.
每次情况都一样。当专家被问及一份从未打开过的文档时，他们只能猜测。而流程也是如此。

Parsing has two layers. This article (5_A) covers the first: knowing the nature of the document (born-digital vs scanned, source software, declared metadata, native TOC if any) and a short LLM-written summary (page count, plus three or four sentences naming the document type, the main subject, the fields it carries). The next article (5_B) covers the second: knowing the content precisely through a relational base where every line, span, image, and TOC entry becomes one row keyed by page and position.
解析分为两层。本文 (5_A) 涵盖第一层：了解文档的本质（原生数字文档与扫描件、源软件、声明的元数据、原生目录）以及由 LLM 撰写的简短摘要（页数，加上三四句描述文档类型、主要主题和所含字段的句子）。下一篇文章 (5_B) 涵盖第二层：通过关系型数据库精确了解内容，其中每一行、每一个跨度、图像和目录条目都成为以页面和位置为键的一行数据。

The article uses PyMuPDF (also imported as fitz), a free Python library that reads PDF bytes directly. No external tools, no API key. Fast enough to run at ingest time and accurate on born-digital PDFs. The same parse_pdf contract can be re-implemented by heavier engines (Azure Layout, Docling, Camelot, vision-LLM fallback). When a page demands more depth than fitz can give, an adaptive cascade dispatches across them. That escalation is a follow-up topic, beyond this article’s scope.
本文使用 PyMuPDF（也称为 fitz），这是一个直接读取 PDF 字节的免费 Python 库。无需外部工具，无需 API 密钥。它在摄入时运行速度快，且对原生数字 PDF 的解析非常准确。同样的 `parse_pdf` 契约可以由更强大的引擎（Azure Layout、Docling、Camelot、视觉 LLM 回退）重新实现。当页面需要比 fitz 更深度的解析时，自适应级联机制会进行调度。这种升级是后续的话题，超出了本文的范围。

### 1. Document-level signals
### 1. 文档级信号

A PDF gives you two kinds of information. Document-level signals: metadata, native bookmarks, declared properties. Page-level content: what each page holds. The parser reads them in that order, and trusts content when the two disagree.
PDF 提供两种信息。文档级信号：元数据、原生书签、声明的属性。页面级内容：每一页包含什么。解析器按此顺序读取，当两者冲突时，以内容为准。

metadata read once at doc level, content walked page by page – Image by author
元数据在文档级读取一次，内容则逐页遍历 —— 图片由作者提供。

Metadata is a handful of fields the PDF hands over in milliseconds. Producer, Creator, native bookmarks, encryption flag. Document-level, no walking pages. You read them at the very start of every parse to make a routing call. Word export → direct extraction. Kofax scan → OCR pipeline. Anything ambiguous → the slower content pass.
元数据是 PDF 在几毫秒内就能提供的少量字段。包括生产者、创建者、原生书签、加密标志。这是文档级的，无需遍历页面。你在每次解析开始时读取它们，以做出路由决策。Word 导出 → 直接提取。Kofax 扫描 → OCR 流程。任何模糊的情况 → 较慢的内容遍历。

Metadata lies sometimes. Ghostscript and qpdf overwrite the upstream Producer field when they recompress, so a Word PDF re-distilled twice will claim to be Ghostscript and tell you nothing about the true origin. The helper exposes both the inferred label and the raw creator_raw / producer_raw strings so downstream rules can argue back.
元数据有时会撒谎。Ghostscript 和 qpdf 在重新压缩时会覆盖上游的“生产者”字段，因此一个被二次蒸馏的 Word PDF 会声称自己是 Ghostscript，而不会告诉你真实的来源。辅助工具同时暴露了推断出的标签和原始的 `creator_raw` / `producer_raw` 字符串，以便下游规则可以进行校验。

### 1.1. Source software
### 1.1. 源软件

A PDF almost always advertises its origin through the Creator and Producer fields. That single signal tells us how hard the rest of the parsing will be, and lets us route to the right strategy before opening any page.
PDF 几乎总是通过“创建者”和“生产者”字段声明其来源。这单一信号告诉我们后续解析的难度，并让我们在打开任何页面之前就能选择正确的策略。

Producers fall into roughly five buckets, ordered from easiest to hardest to parse. “Vector tables” below means tables drawn as native lines + text (the cells survive as data); the opposite is a table flattened into a single image (only OCR can recover the cells).
生产者大致分为五类，按解析难度从易到难排序。下文中的“矢量表格”指由原生线条加文本绘制的表格（单元格作为数据保留）；反之则是被扁平化为单一图像的表格（只有 OCR 才能恢复单元格）。

Office authoring tools (easiest). Microsoft Word, PowerPoint, LibreOffice (Writer / Impress), OpenOffice, Google Docs and Slides exports, Apple Pages and Keynote. They preserve logical structure (headings, lists, paragraphs) with native vector fonts. Direct text extraction works well, reading order is reliable, tables are usually vector tables. The bulk of “office documents” you’ll see in an enterprise corpus.
办公创作工具（最简单）。Microsoft Word、PowerPoint、LibreOffice (Writer / Impress)、OpenOffice、Google Docs 和 Slides 导出、Apple Pages 和 Keynote。它们使用原生矢量字体保留了逻辑结构（标题、列表、段落）。直接文本提取效果良好，阅读顺序可靠，表格通常是矢量表格。这是你在企业语料库中看到的大部分“办公文档”。

Document processors. LaTeX engines (pdfTeX, XeTeX, LuaTeX), Pandoc, Quarto, R Markdown, ReportLab, WeasyPrint. Excellent text fidelity but with their own quirks: hyphenation breaks words across lines, math is rendered as vector paths or images (not extractable text), references and citations have unusual spacing. Tables are vector tables most of the time.
文档处理器。LaTeX 引擎 (pdfTeX, XeTeX, LuaTeX)、Pandoc、Quarto、R Markdown、ReportLab、WeasyPrint。文本保真度极高，但有其特有的怪癖：连字符会将单词断开，数学公式被渲染为矢量路径或图像（无法提取文本），参考文献和引用的间距不寻常。表格大多数时候是矢量表格。

Design and publishing tools. Adobe InDesign, Illustrator, QuarkXPress, Affinity Publisher. Multi-column...
设计与出版工具。Adobe InDesign、Illustrator、QuarkXPress、Affinity Publisher。多栏……