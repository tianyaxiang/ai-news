---
title: "Repairing Shape-Prior Shortcuts in Long-Range Single-Shot Fringe Projection Profilometry"
originalUrl: "https://arxiv.org/abs/2607.11928"
date: "2026-07-15T22:25:45.634Z"
---

# Repairing Shape-Prior Shortcuts in Long-Range Single-Shot Fringe Projection Profilometry
# 修复长距离单帧条纹投影轮廓术中的形状先验捷径问题

**Abstract:** Single-shot fringe projection profilometry (FPP) networks that regress depth directly can exploit a shape-prior shortcut, recovering depth from object boundaries rather than from fringe phase. On a photorealistic synthetic benchmark (15,600 fringe images, 50 objects at 1.5-2.1 m standoff), the best such UNet baseline plateaus at 14.54 mm object mean absolute error (MAE), and neither more data nor more capacity removes the shortcut, because neither changes the hypothesis space the optimizer searches.

**摘要：** 直接回归深度的单帧条纹投影轮廓术（FPP）网络可能会利用“形状先验捷径”，即通过物体边界而非条纹相位来恢复深度。在一个逼真的合成基准测试（包含 15,600 张条纹图像，50 个物体，距离 1.5-2.1 米）中，表现最好的 UNet 基准模型在物体平均绝对误差（MAE）上停滞在 14.54 毫米。增加数据量或模型容量都无法消除这一捷径，因为这两者都无法改变优化器搜索的假设空间。

We introduce PhiCalNet, which outputs a wrapped-phase representation $(\sin\phi, \cos\phi)$ and maps it to depth through a fixed differentiable calibration layer, removing the shape-prior solution architecturally rather than by a loss penalty. Because the single-shot mapping is non-injective without fringe order, PhiCalNet takes the fringe order as auxiliary input, an assumption a sensitivity analysis shows tolerates realistic decoding error; a physics-informed (PINN) baseline with the same physics as a soft penalty yields no gain, isolating the architectural choice as the operative factor.

我们引入了 PhiCalNet，它输出包裹相位表示 $(\sin\phi, \cos\phi)$，并通过一个固定的可微标定层将其映射为深度，从而从架构层面而非通过损失惩罚项消除了形状先验解。由于在没有条纹级数的情况下，单帧映射是非单射的，PhiCalNet 将条纹级数作为辅助输入。敏感性分析表明，这一假设能够容忍现实中的解码误差；而采用相同物理机制作为软惩罚项的物理信息神经网络（PINN）基准模型并未带来性能提升，这证明了架构选择才是起作用的关键因素。

PhiCalNet reduces object MAE 3.3x to 4.46 mm, its residual confined to 0.103% of pixels at the $\pm\pi$ wrap discontinuity, and a three-frame extension reaches 1.16 mm. Two checks agree: interpretability makes phase the most decodable internal feature, and pixel-wise conformal uncertainty quantification, to our knowledge the first for FPP, localizes error at the same discontinuity, where rejecting the top 5% of pixels by snapshot disagreement cuts root-mean-square error by 64% versus 3.5% for the baseline.

PhiCalNet 将物体 MAE 降低了 3.3 倍，达到 4.46 毫米，其残差仅局限于 $\pm\pi$ 相位包裹不连续处的 0.103% 像素点，且通过三帧扩展可达到 1.16 毫米。两项验证结果一致：可解释性分析表明相位是最具可解码性的内部特征；此外，我们首次将像素级共形不确定性量化应用于 FPP，结果显示误差集中在上述不连续处。通过剔除快照差异最大的前 5% 像素，均方根误差降低了 64%，而基准模型仅降低了 3.5%。